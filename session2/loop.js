var digits = [1, 2, 3];

digits.forEach( function(d) {
    console.log( d );
});

// When you are in the shell,
// you may use _ to evaluate the last result
// For example:
//console.log( _.length );  // returns the number of elements in the digits array, which is 3 in this case
